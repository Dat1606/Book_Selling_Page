<% if logged_in? %>
  <% if admin_user? %>
    <%= render "shared/control_bar" %>
    <% end %>
    <% end %>
  <div class="show_book col-md-9">
<% if @received_requests.any? %>
<ol><%= render @received_requests %></ol>
<% end %>
<% if @confirmed_requests.blank? %>
  <h1> There are currently no received (book) requests .</h1>
  <% end %>
</div>
