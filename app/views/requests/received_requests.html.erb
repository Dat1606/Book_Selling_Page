<% if logged_in? %>
<% if @received_requests.any? %>
<h2>Received (Book) Requests</h2>
  <table class="table">
    <thead>
      <tr>
        <th class="col-md-2 text-center">Request name </th>
        <th class="col-md-1 text-center">Status</th>
        <th class="col-md-2 text-center">User name</th>
        <th class="col-md-2 text-center">Book name</th>
        <th class="col-md-2 text-center">Borrow date</th>
        <th class="col-md-2 text-center">Return date</th>
      </tr>
    </thead>

    <tbody>
      <%= render @received_requests %>
    </tbody>
  </table>

<% end %>
<% if @received_requests.blank? %>
  <h1> There are currently no received (book) requests .</h1>
  <% end %>
<% end %>
