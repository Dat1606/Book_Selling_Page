<div class="nav-side-menu col-md-3">
    <div class="brand" style="height: 52px;"><%= link_to image_tag("logo.png", :class => "img_preview"), root_path %></div>
    <i class="fa fa-bars fa-2x toggle-btn" data-toggle="collapse" data-target="#menu-content"></i>

        <div class="menu-list">

            <ul id="menu-content" class="menu-content collapse out">
              <% if logged_in? %>
              <% if admin_user? %>
                <a href="<%= new_category_book_path(1) %>" style="color:white"><li><i class="fa fa-plus"></i> Create a new book </li></a>
                <% end %>
                <% end %>
                <% if logged_in? && admin_user? %>
                 <a style="color:white"><li  data-toggle="collapse" data-target="#products" class="collapsed">
                 <i class="fa fa-calendar-check-o fa-lg"></i> Request Management<span class="arrow"></span>
                </li></a>
                <% else %>
                   <a style="color:white"><li  data-toggle="collapse" data-target="#products" class="collapsed">
                 <i class="fa fa-calendar-check-o fa-lg"></i> My Request<span class="arrow"></span>
                </li></a>
                  <% end %>
                <ul class="sub-menu collapse" id="products">
                    <a href="<%=requests_path %>" style="color:white"><li class="active"> Show all request</li></a>
                    <% if logged_in? %>
                    <% if admin_user? %>
                    <a href="<%= incoming_requests_requests_path  %>" style="color:white"><li> Incoming Requests <span class="badge badge-primary badge-pill"><%= @incoming_requests.count %> </span></li></a>
                    <% else %>
                     <a href="<%= incoming_requests_requests_path %>" style="color:white"><li> Pedding Requests <span class="badge badge-primary badge-pill"><%= @incoming_requests.count %> </span></li></a>
                     <% end %>
                     <% end %>
                    <a href="<%= confirmed_requests_requests_path %>" style="color:white"><li> Confirmed Requests</li></a>
                    <a href="<%= received_requests_requests_path %>" style="color:white"><li> Received Book Requests</li></a>
                    <a href="<%= returned_requests_requests_path%>" style="color:white"><li> Returned Book Requests</li></a>
                    <a href="<%= rejected_requests_requests_path %>" style="color:white"> <li> Rejected Requests</li></a>
                </ul>

                <a href="<%= category_books_path(1) %>" style="color:white"><li><i class="fa fa-book"></i> All books</li></a>
                <% if logged_in? %>
                <% if admin_user? %>
                <a style="color:white"><li data-toggle="collapse" data-target="#service" class="collapsed">
                  <i class="fa fa-globe fa-lg"></i> Categories <span class="arrow"></span>
                </li></a>
                <% end %>
                <% end %>
                <ul class="sub-menu collapse" id="service">
                  <a href="<%= categories_path %>" style="color:white"><li>All categories</li></a>
                  <%#= render Category.where(depth: 1) %>
                </ul>

                <a style="color:white"><li data-toggle="collapse" data-target="#new" class="collapsed">
                  <i class="fa fa-address-card fa-lg"></i> Authors <span class="arrow"></span>
                </li></a>
                <ul class="sub-menu collapse" id="new">
                  <a href= "<%= authors_path %>" style="color:white"><li> All authors</li>
                </ul>
                  <% if logged_in? %>
                 <a href="<%= user_path(current_user) %>" style="color:white"><li>
                  <i class="fa fa-user fa-lg"></i> Profile</li></a>
                  <% end %>
                 <a href="<%= users_path %>" style="color:white"><li>
                  <i class="fa fa-users fa-lg"></i> Users</li></a>
                <% if logged_in? %>
                <% if admin_user? %>
                <a href="<%= signup_path %>" style="color:white"><li><i class="fa fa-plus"></i> Create a new user</li></a>
                 <a href="<%= new_category_path %>" style="color:white"><li><i class="fa fa-plus"></i> Create new category</li></a>
                <% end %>
                <% end %>
            </ul>
     </div>
</div>

<script>
$(document).ready(function(){
  $('.dropdown-submenu a.test').on("click", function(e){
    $(this).next('ul').toggle();
    e.stopPropagation();
    e.preventDefault();
  });
});
</script>
