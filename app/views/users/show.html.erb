<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
       <section class="stats">
        <%= render 'shared/stats' %>
      </section>
    </section>
  </aside>
</div>
 <%= render 'follow_form' if logged_in? %>
<%# if @categories.any? %>
<ol class="microposts">
   <%#= render @categories %>
</ol>
<%# end %>

