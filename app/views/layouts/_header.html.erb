<% if logged_in? %>
<header class="navbar navbar-light" style="background-color:#30323A ;margin-bottom: 0px;">
  <div class="container">
    <%= link_to "Thunder.", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
         <li><%= form_tag(category_books_path(1),  :method => "get", id: "search-form", html: { multipart: true }) do |f| %>
              <%= submit_tag "Search", :name => nil, class: "search2 btn btn-primary" %>
              <%= text_field_tag :search, params[:search], placeholder: "Search book...", class: "search1" %></li>
            <% end %>
          <li class="dropdown">
            <a href="" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
      </ul>
    </nav>
  </div>
</header>
<% else %>
<header class="navbar navbar-light" style="background-color: #563d7c;margin-bottom: 0px;">
  <div class="container">
    <%= link_to "Thunder.", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Log in", root_path %></li>
        <li><%= link_to "Sign up", signup_path %></li>
        <% if logged_in? %>
          <li><%= link_to "Users", '#' %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", '#' %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
<% end %>

