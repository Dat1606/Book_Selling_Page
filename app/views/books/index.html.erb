<% if admin_user? %>
  <%= render "shared/control_bar" %>
<% end %>
<div class="show_book col-md-9">
   <%= form_tag(category_books_path(1),  :method => "get", id: "search-form", html: { multipart: true }) do |f| %>
     <%= submit_tag "Search", :name => nil, class: "search2 btn btn-primary" %>
   <%= text_field_tag :search, params[:search], placeholder: "Search books...", class: "search1" %>

      <% end %>

    <% if @books.blank? %>
    <h4>There are no books containing the term "<%= params[:search] %>".</h4>
  <% end %>
<% if @books.any? %>
<%= render @books %>
<%= paginate @books %>
<% end %>
<div>
