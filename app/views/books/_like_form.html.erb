<div id="like_form<%= book.id %>">
  <% if logged_in? %>
  <% if current_user.like?(book) %>
     <%= render 'books/unlike', book: book %>
  <% else %>
   <%= render 'books/like', book: book %>
  <% end %>
  <% end %>
  </div>
